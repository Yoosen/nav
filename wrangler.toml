name = "cloudnav"
compatibility_date = "2025-01-01"
pages_build_output_dir = "dist"

# 本地开发：KV 绑定（本地持久化到 .wrangler/state）
[[kv_namespaces]]
binding = "CLOUDNAV_KV"
id = "local-cloudnav-kv"

# 本地开发用的管理员密码（部署到 Cloudflare 时改用平台环境变量 PASSWORD 覆盖）
[vars]
PASSWORD = "admin123"
